apiVersion: v1
kind: Service
metadata:
  name: fusion-electronics-frontend
  namespace: fusion-ecommerce
  labels:
    app: fusion-electronics
    component: frontend
spec:
  type: ClusterIP
  selector:
    app: fusion-electronics
    component: frontend
  ports:
    - name: http
      port: 80
      targetPort: http
