apiVersion: v1
kind: Service
metadata:
  name: fusion-electronics-backend
  namespace: fusion-ecommerce
  labels:
    app: fusion-electronics
    component: backend
spec:
  type: ClusterIP
  selector:
    app: fusion-electronics
    component: backend
  ports:
    - name: http
      port: 80
      targetPort: http
